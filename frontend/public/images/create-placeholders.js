// Script para criar placeholders PNG usando canvas (Node.js)
const fs = require('fs');
const path = require('path');

console.log('Para criar placeholders PNG, instale o pacote canvas:');
console.log('npm install canvas');
console.log('Depois execute: node create-placeholders.js');
console.log('\nAlternativamente, use imagens PNG reais dos filmes ou converta as SVGs existentes.');
