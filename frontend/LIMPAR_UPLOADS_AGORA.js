// Cole este código no console do navegador (F12) para limpar uploads imediatamente

// Limpar todos os uploads
localStorage.removeItem('cinevision_upload_tasks');

// Recarregar a página para aplicar
location.reload();
