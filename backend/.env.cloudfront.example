# CloudFront Configuration
# These variables need to be set after CloudFront setup is complete

# CloudFront Distribution Domain (without https://)
CLOUDFRONT_DOMAIN=xxxxx.cloudfront.net

# CloudFront Key Pair ID (from AWS Console)
CLOUDFRONT_KEY_PAIR_ID=APKAXXXXXXXX

# Path to CloudFront private key file
CLOUDFRONT_PRIVATE_KEY_PATH=/path/to/cloudfront-private-key.pem

# CloudFront signed URL TTL in seconds (default: 1 hour)
CLOUDFRONT_SIGNED_URL_TTL=3600

# S3 Configuration (already configured)
AWS_S3_BUCKET=cinevision-videos-prod
AWS_REGION=us-east-1

# Example of complete .env file after CloudFront setup:
# CLOUDFRONT_DOMAIN=d1234567890abc.cloudfront.net
# CLOUDFRONT_KEY_PAIR_ID=APKAI23HZ27SI6FQMGNQ
# CLOUDFRONT_PRIVATE_KEY_PATH=./secrets/cloudfront-private-key.pem
# CLOUDFRONT_SIGNED_URL_TTL=3600